# EvalFlow AI Coding Instructions

이 문서는 AI 코딩 에이전트가 따라야 하는 최우선 프로젝트 규칙이다.

## 목표

빠르게 코드를 많이 만드는 것이 목표가 아니다.
사람 개발자가 장기간 유지보수할 수 있는 구조, 예측 가능한 코드 위치, 명확한 책임, 테스트 가능성을 우선한다.

## 작업 전 반드시 수행

1. `docs/01_PRODUCT.md`
2. `docs/02_DOMAIN.md`
3. `docs/03_ARCHITECTURE.md`
4. `docs/04_SECURITY.md`
5. `docs/05_UI_GUIDELINES.md`
6. 관련 모듈의 기존 코드와 테스트

를 먼저 읽는다.

코드를 즉시 수정하지 말고 먼저 다음을 짧게 보고한다.

1. 요청 기능 재정의
2. 영향을 받는 모듈
3. 수정할 파일
4. 새로 만들 파일
5. Domain/Data model 변경 여부
6. 권한/보안 영향
7. 테스트 계획

## 절대 금지

- 500~1000줄짜리 거대한 `page.tsx` 또는 단일 컴포넌트 생성
- React 컴포넌트 안에 평가 계산/검증 규칙 작성
- 브라우저에서 Firebase Admin SDK 사용
- 브라우저에서 AI API secret 사용
- `any`, `@ts-ignore`, 이유 없는 강제 타입 캐스팅
- 오류를 숨기기 위한 `eslint-disable`
- catch 후 예외 무시
- 학교명·교과명·학년도·정기고사일을 코드에 직접 하드코딩
- `if (subject === "영어")` 방식으로 업무 차이를 해결
- Teacher/Admin용 동일 로직 복사 구현
- 모든 보조함수를 `utils.ts` 하나에 누적
- 필요 없는 Redux/Zustand 등 전역 상태 라이브러리 추가
- 동일 목적의 라이브러리 중복 설치
- AI가 생성한 HTML/JS/React 코드를 DB에 저장해 실행
- `eval`, `new Function` 등 동적 코드 실행
- AI 결과를 검증 없이 저장하거나 사용자 승인 없이 공식 평가계획에 반영
- 테스트 실패를 숨기기 위해 테스트를 삭제/완화
- 관련 없는 리팩터링을 기능 작업과 동시에 수행
- 현재 인증 방식이 미정인데 가짜 이메일 등의 편법으로 로그인 ID를 구현

## 모듈 경계

`src/app`은 라우팅과 화면 조립만 담당한다.
업무 기능은 `src/modules/*`에 둔다.
기술 공통요소는 `src/shared/*`에 둔다.

다른 모듈의 내부 파일을 deep import하지 않는다.
각 모듈의 `index.ts`가 외부 공개 API를 결정한다.

## 의존 방향

UI → Application → Domain

Domain은 React, Next.js, Firebase를 import하지 않는다.
Domain 함수는 Node 환경에서 단독 테스트 가능해야 한다.

## 데이터와 출력 분리

평가 데이터는 PDF/HWPX/학교별 표 모양과 무관하게 설계한다.
출력 형식 때문에 Domain 모델을 왜곡하지 않는다.

## 수행평가 확장성

수행평가는 `Section[] + ScoringModel` 조합으로 모델링한다.
새로운 점수 유형을 추가할 때 기존 유형을 수정하는 대신 registry에 새 모델을 등록하는 방향을 우선한다.

## AI 사용 원칙

AI가 맡을 수 있는 것:
- 전년도 문구 잔존 검토
- 타 교과 문구 잔존 검토
- 문장/평가기준 모호성 검토
- 의미적 연계성에 대한 제안

AI가 맡지 않는 것:
- 반영비율 합산
- 날짜/요일 계산
- 존재하는 성취기준 여부의 최종 판정
- 역할/권한 판정
- 공식 평가 데이터 자동 수정

## UI 규칙

- 이모지 사용 금지
- 랜딩페이지식 홍보 문구 금지
- 장식성 그라데이션/과도한 그림자/유리효과 금지
- 시스템 기본 글꼴 우선
- 표와 폼 중심의 업무 UI
- 버튼/메뉴 이름은 짧고 업무 용어 그대로 사용
- 일반 교사에게 관리자 기능을 노출하지 않는다

## 작업 완료 조건

- `npm run typecheck` 성공
- `npm run lint` 성공
- 관련 테스트 성공
- 가능하면 `npm run build` 성공
- 요청하지 않은 기능 변경 없음
- 하드코딩 추가 없음
- 보안 경계 우회 없음

완료 후 변경 파일, 테스트 결과, 남은 위험/TODO를 보고한다.

## 19. ScoringModel 확장 규칙

새로운 수행평가 유형이 필요한 경우 교과명/학교명 조건문을 추가하지 않는다.
`docs/11_SCORING_MODEL_EXTENSION_GUIDE.md` 절차에 따라 ScoringModel 자체를 확장한다.

ScoringModel 구현을 런타임 플러그인 시스템으로 과도하게 일반화하지 않는다.
현재 구조에서는 유형별 독립 구현 + 명시적 registry/switch 연결을 사용한다.
