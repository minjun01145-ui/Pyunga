# 개발 규칙

이 문서는 EvalFlow의 구현과 유지보수 기준을 정리합니다. 기능 구현 속도보다 코드의 일관성, 가독성, 변경 용이성을 우선합니다.

## 작업 전 확인

변경 전 다음 문서와 관련 모듈의 기존 코드·테스트를 확인합니다.

- `docs/01_PRODUCT.md`
- `docs/02_DOMAIN.md`
- `docs/03_ARCHITECTURE.md`
- `docs/04_SECURITY.md`
- `docs/05_UI_GUIDELINES.md`

큰 변경은 바로 구현하지 않고 변경 범위, 데이터 모델 영향, 권한 영향, 테스트 방법을 먼저 정리합니다.

## 구현 원칙

- 가장 단순하게 현재 요구사항을 만족하는 구현을 우선합니다.
- 실제 요구가 없는 범용 프레임워크, 플러그인 체계, 래퍼 계층을 미리 만들지 않습니다.
- 한 번만 쓰이는 코드를 억지로 추상화하지 않습니다.
- 같은 업무 규칙이 여러 화면에서 반복되면 Domain 또는 Application 계층으로 올립니다.
- 기능 작업과 관계없는 대규모 리팩터링을 한 커밋에 섞지 않습니다.
- 사용하지 않는 필드, 인터페이스, 확장 포인트를 미래 가능성만으로 추가하지 않습니다.

## 모듈 경계

- `src/app`: 라우팅과 화면 조립
- `src/modules/*/domain`: 프레임워크와 무관한 업무 개념·규칙
- `src/modules/*/application`: 사용자 업무 단위
- `src/modules/*/infrastructure`: Firebase, 외부 API, 파일 출력 등 기술 구현
- `src/modules/*/ui`: React 화면
- `src/shared`: 업무 의미가 없는 공통 요소

Domain은 React, Next.js, Firebase를 import하지 않습니다. 다른 모듈의 내부 경로를 직접 참조하지 않고 공개 API를 통해 사용합니다.

## TypeScript

- `any`, `@ts-ignore`, 오류 회피 목적의 강제 타입 캐스팅을 사용하지 않습니다.
- 문자열 입력을 union 타입으로 바꿀 때 값 검증 없이 `as`로 단정하지 않습니다.
- 상태를 여러 boolean으로 중복 표현하지 않습니다.
- 계산 가능한 값은 중복 저장하지 않습니다.
- 오류는 가능한 경우 문자열이 아니라 명시적인 오류 코드와 타입으로 표현합니다.

## React

- 컴포넌트는 표시와 사용자 입력 전달에 집중합니다.
- 평가 계산, 반영비율 검증, 상태 전이 같은 업무 규칙을 컴포넌트에 넣지 않습니다.
- 한 컴포넌트가 여러 책임을 갖기 시작하면 역할 단위로 분리합니다.
- 중첩된 불변 상태 갱신 코드가 반복되면 의미 있는 갱신 함수로 추출합니다.
- 한 번만 쓰이는 단순 코드를 위한 범용 helper는 만들지 않습니다.
- 서버에서 처리할 수 있는 일을 습관적으로 Client Component로 만들지 않습니다.

## Firebase 및 보안

- 브라우저에서 Firebase Admin SDK를 사용하지 않습니다.
- 서버 권한 검증 없이 관리자 작업을 수행하지 않습니다.
- 비밀값을 저장소에 올리지 않습니다.
- 학교명, 교과명, 학년도, 시험일 같은 운영 데이터를 코드에 박지 않습니다.
- 계정 체계가 정해지지 않은 상태에서 가짜 이메일 같은 편법으로 인증을 구현하지 않습니다.

## 수행평가 모델

- 교과명이나 학교명을 조건문으로 검사하여 평가 방식을 나누지 않습니다.
- 수행평가는 `EvaluationSection`과 `ScoringModel` 조합으로 표현합니다.
- 새 유형이 필요하면 실제 사례가 기존 모델로 표현되지 않는지 먼저 확인합니다.
- 새 유형 추가 시 기존 유형의 동작을 바꾸기보다 명시적인 타입과 검증 로직을 추가합니다.
- 현재 규모에 불필요한 런타임 플러그인 시스템은 만들지 않습니다.

## 주석과 문서

- 코드를 그대로 번역하는 주석은 쓰지 않습니다.
- 주석은 선택 이유, 업무 규칙의 근거, 이해하기 어려운 제약을 설명할 때만 사용합니다.
- `현재 단계에서는`, `나중에 구현`, `임시로`, `향후 처리` 같은 개발 이력 설명을 제품 코드 주석에 남기지 않습니다. 필요한 내용은 이슈나 설계 문서에 기록합니다.
- 긴 설명형 주석보다 읽기 쉬운 함수·타입·변수 이름을 우선합니다.
- 사용자가 보는 화면에 내부 기술명, 개발 단계, 구현 계획을 노출하지 않습니다.

## UI

- 학교 업무용 프로그램에 맞는 단순한 표·폼 중심 화면을 유지합니다.
- 이모지, 장식성 그라데이션, 과도한 그림자와 애니메이션을 사용하지 않습니다.
- 개발자용 설명이나 내부 아키텍처를 사용자 화면에 표시하지 않습니다.
- 버튼과 메뉴는 실제 학교 업무에서 쓰는 짧은 용어를 사용합니다.

## 의존성

새 패키지를 추가하기 전에 표준 API나 현재 의존성으로 해결 가능한지 확인합니다. 같은 목적의 라이브러리를 중복 도입하지 않습니다.

## 테스트

업무 규칙을 변경하면 관련 테스트도 함께 변경합니다. 버그를 수정할 때는 가능하면 버그를 재현하는 테스트를 먼저 추가합니다.

완료 전 다음을 확인합니다.

```bash
npm run typecheck
npm run lint
npm run test:run
npm run build
```

## 저장소 관리

- 커밋 메시지는 변경 목적을 알아볼 수 있게 작성합니다.
- 생성물, 캐시, 비밀값을 커밋하지 않습니다.
- 사용하지 않는 코드와 파일은 이유 없이 남겨두지 않습니다.
- 공개 시제품에 미완성 기능을 노출해야 한다면 내부 구현 설명 대신 사용자 관점의 간단한 상태만 표시합니다.
- 최종 제출 전 개발용 fixture, raw JSON 보기, 단계별 체크리스트 등 심사에 필요하지 않은 개발 보조물을 별도로 점검합니다.
